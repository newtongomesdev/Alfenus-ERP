export type SignatureEnvelopeStatus = "draft" | "prepared" | "sending" | "sent" | "viewed" | "partially_signed" | "signed" | "refused" | "expired" | "cancelled" | "failed";
export type SignatureSignerType = "person" | "organization_representative" | "internal_witness" | "external_witness";
export type SignatureSignerStatus = "pending" | "ready" | "removed";
export type SignatureSigner = { id?: string; signerType: SignatureSignerType; role: string; name: string; email: string; phone?: string | null; taxIdentifier?: string | null; organizationName?: string | null; signingOrder: number; requiresIdentityVerification: boolean; status?: SignatureSignerStatus };
export type SignatureSource = { contractId: string; contractDocumentId: string; contractVersionId: string; status: string; documentHash: string | null; fileSize: number | null; pageCount: number | null; title: string; firmName?: string | null; parties?: Record<string, unknown>; createdAt?: string | null; rendererVersion?: string | null; templateVersion?: string | null; stale?: boolean; archived?: boolean };
export type SignatureEnvelope = { id: string; lawFirmId: string; contractId: string; contractDocumentId: string; contractVersionId: string; status: SignatureEnvelopeStatus; title: string; consentVersion: string; signingOrderEnabled: boolean; expiresAt: string | null; preparedAt: string | null; cancelledAt: string | null; documentHash: string; documentFileSize: number; documentPageCount: number; lockVersion: number; signers: SignatureSigner[]; snapshot: Record<string, unknown>; };
